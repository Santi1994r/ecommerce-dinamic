//Ejercicio D-Solicitar el usuario y la contraseña de login y mostrarla por consola(pueden ser inventados). Estos datos no pueden venir vacios. Realizar un ciclo while o do while para resolverlo.

/* do {
    let usuario = "";
    let contrasenia = "";
    let askAgain = true;
    usuario = prompt("Ingrese su nombre");
    contrasenia = prompt("Ingrese su contraseña");
    if (!usuario || !contrasenia) {
        alert("No puedes ingresar un dato vacio. Reintente");
    } else {
        alert(`Usuario ${usuario} con la contraseña ${contrasenia} fueron guardados con exito.`);
        askAgain = false;
    }
} while (askAgain); */